<div class="container">
  <div class="text-center">
    <h1 class="fw-bold">Ma location</h1>
    <h2><%= @location.name %></h2>
  </div>
  <div class="rounded shadow p-3 bg-light">
    <div class="text-center">
    <h2><%= @location.game.name %></h2>
    </div>
    <div class="d-flex justify-content-between">
      <div class="w-50">
        <p><%= @location.game.description %></p>
      </div>
      <p class="d-flex align-items-end fw-bold"><%= "du #{@location.start} au #{@location.end}" %></p>
    </div>
  </div>
</div>
