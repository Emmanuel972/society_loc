
<div class="container text-center">
  <h1 class="p-5">Society loc</h1>
  <div class="text-center p-3">
    <div class="cards_game">
      <% @games.each do |game| %>
        <div class="col m-3">
          <%= link_to  game_path(game) do %>
          <%= render "composents/game_card", game: game %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
